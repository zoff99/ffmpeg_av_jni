name: macos JNI Lib

on:
  push:
    paths-ignore:
     - 'README.md'
     - 'CHANGELOG.md'
  pull_request:
    paths-ignore:
     - 'README.md'
     - 'CHANGELOG.md'
  workflow_dispatch:
    inputs:
      version:
        description: dummy
        default: dummy

defaults:
  run:
    shell: bash

jobs:

  build-macos-nightly-artifact:
    name: Nightly Artifact macOS Build
    runs-on: macos-13
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - name: show architecture
        run:  uname -m

      - name: install deps
        run:  brew install git automake yasm nasm

      - name: build deps
        run:  |
          ls -al
          id
          pwd
          ./ci_scripts/deps_macos.sh

      - name: build jni lib
        run:  |
          ls -al
          id
          pwd
          ./ci_scripts/java_jni_lib_macos.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: libffmpeg_av_jni.jnilib
          path: libffmpeg_av_jni.jnilib
      - name: Upload to nightly release
        uses: ncipollo/release-action@v1
        if: github.ref == 'refs/heads/master'
        with:
          allowUpdates: true
          tag: nightly
          omitBodyDuringUpdate: true
          omitNameDuringUpdate: true
          prerelease: true
          replacesArtifacts: true
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: "libffmpeg_av_jni.jnilib,test_macos"

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: test_macos
          path: test_macos


